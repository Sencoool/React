import React from "react";
import ReactDOM from "react-dom/client"; // Adjust component
import App from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
); //App use call component of React
